"""Latent Recall Dynamics (LRD) — a pre-generation knowledge-boundary estimator.

A small sidecar predicts, from a prompt's input-pass activations only, whether the base
model's parametric knowledge is sufficient to answer correctly. See README.md.
"""

__version__ = "0.1.0"
