# Reused from the existing system Python 3.12 environment (see README).
# Versions present at build time are noted for reproducibility.
torch>=2.4          # 2.8.0 (MPS build)
transformers>=5.9   # must include Gemma4 support
datasets>=4.0
numpy>=2.0
pandas>=2.0
scikit-learn>=1.4
pyyaml>=6.0
tqdm>=4.60
plotly>=6.0
gradio>=5.0
matplotlib>=3.8
pyarrow>=15.0
psutil>=5.9
