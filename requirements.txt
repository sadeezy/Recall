# Install into a fresh venv:  pip install -r requirements.txt
# Tested on Python 3.12 (macOS / Apple Silicon, MPS). The version after each
# package is the one used to produce the shipped artifacts (see README "Setup").
torch>=2.4          # 2.8.0 (MPS build)
transformers>=5.9   # must include Gemma4 support
datasets>=4.0
numpy>=2.0
pandas>=2.0
scikit-learn>=1.4
pyyaml>=6.0
tqdm>=4.60
plotly>=6.0
gradio>=5.0
matplotlib>=3.8
pyarrow>=15.0
psutil>=5.9
